setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: heroku-redis
      as: REDIS
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - bundle exec rails db:migrate
run:
  web: bundle exec rails server -b '0.0.0.0' -p $PORT
